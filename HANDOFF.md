# deploy_shop 交接说明

> 状态日期：2026-09-04

## 当前状态

- 项目为抖音带货素材库静态网页，线上地址为 <https://xianyi-shop.surge.sh>。
- 页面入口为 `index.html`，构建后样式、脚本和音频索引分别位于 `assets/site.css`、`assets/site.js`、`data/audio-index.json`。
- 当前素材按商品标签组织，包含新号素材池、商品账号素材、开头钩子、视频 OCR 文案、原声拍同款入口和内容包复制等能力。
- 项目根目录中的 JSON 文件提供音频配置、体育热点素材、新号池、钩子包和知识库等数据；`media/` 保存账号头像。
- 公网页面不内嵌任何付费接口密钥。AI 回复助手保留界面、商品匹配和知识库逻辑，但公网生成开关关闭，提示使用本地工具。

## 已实现功能

- 商品标签切换及标签素材数量动态统计。
- 音频组、账号卡、视频卡展示，以及钩子切换、文案复制、内容包复制和抖音原声跳转。
- 音频指纹与重复音频标记，重复素材不计入可见音频数量。
- “已发/未发”本地状态管理及仅看未发素材筛选。
- 新号素材池随机抽取、剩余数量统计和本地使用记录。
- OCR 文案覆盖配置加载。
- AI 回复助手的商品识别、禁忌提醒、知识库检索和合规提示；公网版本不执行远程生成。

## 近期改动

- 页面及素材数据更新到 2026-09-04 本地快照，`index.html` 最近修改时间为 2026-09-04 00:12:27。
- 增加并拆分 `assets/site.css`、`assets/site.js` 与 `data/audio-index.json` 构建产物。
- 更新 `audio_config.json`、`sports_pack.json`、`fresh_sports_pack.json`、`hot_sports_pack.json`、`newacc_pool.json`、`hooks_pack.json` 等素材数据。
- 补充账号头像素材，并保留下线素材的历史数据结构。
- 清理公网付费接口密钥，使用 `LLM_ENABLED` 控制 AI 回复生成能力。

## 验证情况

- 部署脚本会先执行 `scripts/build_shop.mjs`，检查构建产物是否存在，并校验 `index.html` 的 `<div>` 开闭数量。
- 部署后脚本会访问线上首页，检查响应体大小，并抽查开运堂和姜枣茶的可见音频组数量。
- 本次交接会执行 JavaScript 语法检查、JSON 解析检查、HTML `div` 平衡检查和 Git 暂存内容检查；结果以本次 Git 提交回报为准。
- 本次任务只整理文档并提交现有本地快照，未改动页面业务代码，因此不触发重新部署。

## 下一步

1. 每次修改 `index.html` 后运行 `C:\Users\Administrator\.openclaw\workspace\scripts\deploy_shop.ps1`，并保留脚本的构建、结构校验与线上抽查结果。
2. 新增素材时同步维护对应 JSON 数据，运行构建脚本后检查标签数量和重复音频标记。
3. 若恢复 AI 回复生成，应使用本地或服务端安全代理，禁止在静态网页中嵌入密钥。
4. 建议后续为构建脚本增加固定的离线回归检查，覆盖 JSON schema、核心商品标签和复制按钮。

## 已知问题

- 公网 AI 回复生成功能当前关闭，这是安全设计；页面只能展示维护提示，实际生成需使用本地工具。
- `ocr_config.json` 当前为空对象，暂时没有额外 OCR 覆盖配置。
- 数据源较多，部分素材包字段结构不同，更新时需由构建逻辑统一处理，避免直接手工拼接页面。
- 本次提交前工作区包含较多长期未跟踪文件；本次以完整项目快照方式纳入版本管理。

## 安全约束

- 禁止在文档、网页、脚本和数据文件中写入任何密钥、Cookie 或令牌。
- 禁止将本地敏感配置和抓包文件纳入 Git。

